apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "magento-chart.fullname" . }}-test-connection"
  labels:
    {{- include "magento-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "magento-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
