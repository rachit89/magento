apiVersion: v1
kind: Service
metadata:
  name: {{ include "magento-chart.fullname" . }}
  labels:
    {{- include "magento-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magento-chart.selectorLabels" . | nindent 4 }}
